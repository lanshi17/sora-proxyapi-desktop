2026-03-20: Scaffolded a Tauri 2 + React + TypeScript desktop shell with Vitest coverage so the MVP has a verified project foundation. Current state: app shell, persisted settings form, typed generation form, Tauri-backed image picker, packaging config, and build/test verification are in place; upload/linxi workflow remains blocked by the missing upload API contract.
2026-03-20: Added macOS packaging config and helper script so internal-test bundle settings are checked into the repo. Current state: `npm test` and `npm run build` pass, but `npm run tauri build -- --bundles app` is invalid in the current Linux environment and macOS `.app` output is still unverified here.
2026-03-20: Implemented the real png.cm upload client using `@tauri-apps/plugin-fs` so local image paths are converted into uploaded public URLs before Linxi create calls. Current state: upload requests send real file bytes with token + `image` multipart fields, upload tests are green, and upload flow is wired into the app.
2026-03-20: Finished the Linxi generate workflow so the app now uploads images, creates tasks, polls query status, and opens completed video results from the Generate mode. Current state: app workflow, Linxi create/query clients, and polling state machine are implemented and verified.
2026-03-20: Restructured the product into a Generate-first Linxi Video Studio with top-level `Generate`, `Iterate`, and `Storyboard` modes. Current state: Generate is fully functional and productized, Iterate/Storyboard ship as honest placeholder workspaces, and model capability metadata exists for `sora-2`, `sora-2-pro`, future `veo`, and future `seedance`.
2026-03-20: Added browser-safe image-picker fallback, reactive settings usage, and a lightweight Recent Jobs surface so local development and session-level workflow visibility are stable. Current state: the app works in both Tauri runtime and plain browser dev, saved settings apply without restart, recent jobs appear in Generate mode, and the redesign is complete through Task 9.
2026-03-21: Expanded model catalog and implemented remix client based on OpenAPI analysis so Iterate mode is unblocked. Current state: added Grok (grok-video-3), Luma (ray-v1, ray-v2), Kling (kling-v1, kling-v1-6, kling-v2, kling-v2-6), and Runway (gen4_turbo, gen3a_turbo) models with provider-specific capabilities; created linxi-remix-client.ts for POST /v1/videos/{id}/remix endpoint; all 66 tests pass.
8: 2026-03-22: Implemented VideoPreview component with dual-mode video download (Tauri save dialog / browser download) using test-driven development. Current state: created src/features/video/ with VideoPreview.tsx (React component), video-download.ts (platform-aware utility), and video-preview.test.tsx (2 passing tests); all 83 tests pass; build verified clean.
